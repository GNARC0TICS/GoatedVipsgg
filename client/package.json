//This is a dummy file as no original code was provided.  The changes provided only modify a build script.  A complete application would be required to implement the fix fully.
{
  "scripts": {
    "dev:client": "vite --host 0.0.0.0",
    "build:client": "vite build",
    "dev:server": "nodemon server.js",
    "build:server": "node build/server.js"

  }
}